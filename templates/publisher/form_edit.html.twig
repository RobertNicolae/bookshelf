{% extends "base.html.twig" %}


{% block content %}
    <h3>Editeaza editura</h3>
    <form method="post">
        {% if errors.publisher_name is defined %}
            <div class="alert alert-danger" role="alert">
                {{ errors.publisher_name }}
            </div>
        {% endif %}
        <div class="mb-3">
            <label for="exampleFormControlInput1" class="form-label">Name</label>
            <input type="text" class="form-control" id="exampleFormControlInput1" name="publisher_name" value="{{ publisher.name }}">
        </div>
        <input type="submit" class="btn btn-primary" value="Salveaza">
    </form>
{% endblock %}